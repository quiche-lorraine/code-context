parameters:
    level: 6
    paths:
        - src
        - tests
    excludePaths:
        # Sample user-project files used as test fixtures: they intentionally
        # reference Doctrine/Symfony classes that are not installed here.
        - tests/Fixtures/*
    tmpDir: .phpstan
